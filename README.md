# Airflow-ETL-pipeline
This project demonstrates a production-style ETL (Extract, Transform, Load) pipeline built using Apache Airflow. It processes multiple CSV datasets, performs data cleaning and transformation, and loads the final dataset into a database.
